<%= form_for [@resource, @comment] do |f| %>
  <%= f.label :comment %>
  <%= f.text_area :comment %>
  <%= f.rating :rating %>
  <%= f.select :rating, (1..5) %>
  <%= f.submit "Post comment" %>
<% end %>
